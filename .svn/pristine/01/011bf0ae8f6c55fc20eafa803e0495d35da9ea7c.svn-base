 /**
  *author: Triones
  *date	 : 2014-08-25
  *desc  :socket 共用方法
  */
#ifndef _TRIONES_SOCKUTIL_H_
#define _TRIONES_SOCKUTIL_H_
#include "common.h"

namespace triones
{
	extern int set_non_block(int sock_fd);

	extern bool set_reuse_addr(int socket);

	extern bool set_recv_buf( int fd,int max);

	extern bool set_send_buf( int fd,int max);

	extern int get_socket_error(int sock_fd);
}
#endif /* _TRIONES_SOCKUTIL_H_ */
